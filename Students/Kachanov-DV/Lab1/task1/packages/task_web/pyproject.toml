[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "task-web"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["fastapi>=0.112,<0.118"]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
